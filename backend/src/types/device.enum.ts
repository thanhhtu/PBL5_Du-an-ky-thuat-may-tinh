export enum DeviceState {
  ON = 'on',
  OFF = 'off'
}

export enum DeviceAction {
  TURN_ON = 'turn_on',
  TURN_OFF = 'turn_off'
}

export enum DeviceName {
  DOOR = 'door',
  CURTAIN = 'curtain',
  FAN = 'fan',
  LIGHT = 'light',
}